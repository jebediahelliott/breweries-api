{
  "name": "breweries",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
    "npm": "6.9.0"
  },
  "scripts": {
    "build": "sh -c 'cd client && npm run install && npm run build'",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "npm run build && npm run deploy"
  }
}
